import os

!git clone https://github.com/huggingface/peft.git
os.chdir("/content/peft")
print(f"Change the directory to peft")
!ls
!git fetch origin pull/2644/head:pr-2644
!git checkout pr-2644
!pip uninstall peft
!pip install -e . -q
os.chdir("/content")
print(f"Change the directory back to the content")
!ls

!git clone https://github.com/RistoAle97/centered-kernel-alignment.git
os.chdir("/content/centered-kernel-alignment")
print(f"Change the directory to centered-kernel-alignment")
!ls
!pip install -e . -q
os.chdir("/content")
print(f"Change the directory back to the content")
!ls


###
You should run this to use Taha's arrow routing and general knowledge subtraction.

##
The second github is for CKA purposes.